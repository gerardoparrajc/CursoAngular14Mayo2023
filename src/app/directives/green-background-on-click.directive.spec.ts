import { GreenBackgroundOnClickDirective } from './green-background-on-click.directive';

describe('GreenBackgroundOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new GreenBackgroundOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
