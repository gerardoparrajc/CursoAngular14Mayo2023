import { GatoBotasDirective } from './gato-botas.directive';

describe('GatoBotasDirective', () => {
  it('should create an instance', () => {
    const directive = new GatoBotasDirective();
    expect(directive).toBeTruthy();
  });
});
